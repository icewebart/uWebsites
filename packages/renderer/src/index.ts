// Renderer: block tree + branding tokens -> static HTML/CSS artifact (ADR-004).
// TODO: renderPage(blocks, tokens) -> string; renderSite(workspace) -> artifact.
export const RENDERER_TODO = 'block tree + tokens -> static HTML/CSS' as const
