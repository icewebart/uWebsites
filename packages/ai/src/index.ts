// AI orchestration: article pipeline + edit tool-calls (Anthropic).
// TODO: generateArticle(plan), editPage(blockTree, instruction) via tool calls.
export const AI_TODO = 'article pipeline + structured edit tool-calls' as const
