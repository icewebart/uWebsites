export default function Page() {
  return (
    <main className="wrap">
      <div className="brand"><span className="mk">u</span> uWebsites</div>
      <div className="card"><h1>Dashboard</h1><p>The editor + workspaces console will live here (apps/web). Auth via the API at <code>api.uwebsites.net</code> (jose cookie on <code>.uwebsites.net</code>).</p><p><a href="https://app.uwebsites.net/onboarding/">Onboarding prototype →</a></p></div>
    </main>
  )
}
